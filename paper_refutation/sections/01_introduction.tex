% §1 INTRODUCTION — hook: "edit magnitude operationally DOMINATES geometry for edit risk"
% PLAN: lead with (i) the measurement-validity finding C4b (matched-pair selection
%   benchmarks silently leak realized magnitude: low-rho wins 0.765 on smaller edits
%   vs 0.287 on larger) and (ii) magnitude dominance.
% Contributions:
%   (1) exact forward-mode geometry instrument (StyleGAN + Diffusion);
%   (2) ρ encodes semantics (encoder-correlated; magnitude-confounded);
%   (3) rho is NOT an actionable per-direction edit-risk signal — apparent residual
%       is an attribute-difficulty/magnitude-axis artifact (within-unit ≈0:
%       +0.07/+0.16/−0.08; reverses under LPIPS control −0.19/−0.38/−0.11);
%   (4) the matched-pair magnitude-leak measurement-validity caution + protocol.
% BANNED: "sufficient statistic"; "flat"; "real-but-weak ordinal residual"; n=2221
%   as flat; single-seed 92.86% as lead; compositional-editing payoff.
% Numbers ONLY from committed data/*.json. (prose pending Steps 0-3.)
