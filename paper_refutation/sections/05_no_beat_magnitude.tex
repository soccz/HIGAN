% §5 ρ DOES NOT BEAT MAGNITUDE PER-DIRECTION
% The apparent ρ→damage residual is a between-attribute difficulty proxy:
%   pooled partial-Spearman +0.27/+0.48/+0.20 collapses to within-(seed,attr)
%   +0.07/+0.16/−0.08 and REVERSES under total-image-movement (LPIPS) control
%   (−0.19/−0.38/−0.11). Reconcile 0/41 (leaky 4-feature max) vs 6/6 (clean
%   abs_alpha) as a metric/threshold artifact. C3 null-sensitive (FDR table).
% Source: data/residual_conservative.json, rho_incremental_over_realized.json,
%   leakage_audit.json. NOT "adds nothing" as bare assertion — show the artifact.
