# INTERACTION — 개입 반응을 해석하는 방법론

이 파일을 연구의 기준 문서로 사용한다. 방향, 현재 판정, 다음 작업을 여기서 갱신한다.

**종료 상태 (2026-09-07):** 사용자와 논문 확장 여부를 검토한 뒤, 이번 연구는
검증한 범위의 결과·코드·재현 기록 공개로 마무리하기로 했다. 같은 설정의 추가 실험은 종료한다.
[짧은 보고서](research/interpretation_method/REPORT.md)와
[연구 소개 페이지](https://soccz.github.io/projects/higan-interaction/)에서 핵심 결론을 읽을 수 있다.
후속 연구는 기존 문헌과 구별되는 질문과 반증 기준을 먼저 정한 별도 프로토콜로 시작한다.

## 현재 결론 — 검증한 범위에서 기본 방법 확정

**가까운 개입을 한 번 관측하고, 위험 크기를 보정해 예측한 뒤,
허용하기 어려운 경우 설명을 보류하는 방법을 기본으로 채택한다.**

새 기준점 64개·21,504건에서 56.50%를 채택했고, 채택 중 오판은 2.43%,
기준점 bootstrap의 단측 95% 상한은 3.05%였다. 정한 목표(채택≥40%, 오판 상한≤5%)를 통과했다.
view를 제외해도 채택 49.78%, 오판 1.62%로 같은 목표를 통과했다.

두 관측의 크기는 위험 크기 예측 MAE를 10.75% 더 줄이므로 정밀 추정용 선택지로 남긴다.
채택 판단 개선은 작고 호출 수는 177→305회로 증가해 기본 구성에는 넣지 않는다.
방향 정보의 추가 MAE 개선은 0.46%로, 정해 둔 실용 효과 5%보다 작았다.

실행 파일은 [explain.py](research/interpretation_method/explain.py)다.
작은 공동 개입 0.03만 사용해 177회 호출로 336개 판단을 내리고, 목표 공동 출력은 조회하지 않는다.
검증 실험과 336개 점수·판단이 모두 일치했다. **현재 bedroom 생성기·8방향·세 목표 크기의
경험적 방법론 결론**이며, 개별 사례의 무오판이나 보편적 신규 이론을 주장하지 않는다.

## 목표

**모델 내부 표현의 중첩·상쇄·비선형 상호작용을 구분하고,
그 설명이 어느 개입 범위에서 성립하는지 밝힌다.**

연구 대상은 방법론·해석론이다. 기존 생성모델은 이를 검증하는 실험 대상이다.
이미지 편집 제품의 품질 향상을 주목표로 삼지 않는다.

## 해석할 것

1. **개별 반응:** A와 B를 각각 바꾸면 무엇이 얼마나 달라지는가?
2. **중첩·상쇄:** 같은 출력 성분을 강화하거나 서로 상쇄하는가?
3. **비선형 상호작용:** 함께 바꾼 결과에 개별 효과의 합으로 설명되지 않는 변화가 있는가?
4. **적용 범위:** 위 설명이 어떤 기준점·개입 크기·관측 방법에서 유지되는가?

원출력과 관측기의 영향을 구분한다. 혼합 미분이나 고전적 분해 항등식 자체를
신규성으로 주장하지 않는다. 기존 방법보다 해석의 판단을 개선하는지 검증한다.

## 완료한 단계

- 기존 실험을 검토해 C4 상호작용 해석을 첫 주축으로 선택했다.
- 실제 측정 구현의 반례·해석해 검사 11개 통과.
- 첫 bedroom 파일럿: 기준점 24개, 방향 쌍 28개, 총 8,064개 측정 완료.
- 반복 출력과 원래 생성기의 출력 차이 0, 저장 사례의 독립 재계산 차이 0.
- 상대 혼합 반응 중앙값: 개입 0.25배 17.7%, 0.5배 24.8%, 1배 32.5%.

첫 파일럿의 10% 허용 기준 통과는 9/8,064개뿐이다. 이 자료에서 거의 항상
비가산이라고 예측하는 것만으로 높은 정확도를 얻을 수 있으므로 예측 성공으로 세지 않는다.

## 완료 — 작은 개입 범위 조사

상태: **개발용 범위 조사와 결과 검증 완료.** 프로토콜은 결과를 보기 전에 고정했다.

- 기존 개발용 기준점 8개만 사용한다. 새 최종 평가용 기준점은 사용하지 않는다.
- 기존 8방향·28쌍·4부호 조합을 유지한다.
- 개입 크기: 0.01, 0.03, 0.06, 0.1, 0.25, 0.5.
- 상대 허용 오차 10%, 절대 허용 오차 `1e-6`을 그대로 유지한다.
- 가산 설명이 성립하는 구간과 깨지는 구간, 작은 반응의 수치 분해능을 확인한다.
- 이것은 개발용 범위 조사다. 미관측 반응 예측이나 논문 효과의 검증으로 보고하지 않는다.

총 5,376개 측정, 생성 호출 6,152회로 다음 결과를 얻었다. 각 크기당 896개 사례다.
가산 설명은 두 개별 반응을 더해 동시 개입의 반응을 설명하는 것을 뜻한다.

| 개입 크기 | 상대 혼합 반응 중앙값 | 가산 설명 허용 기준 통과율 |
|---|---:|---:|
| 0.01 | 3.42% | 96.21% |
| 0.03 | 6.02% | 88.73% |
| 0.06 | 8.67% | 70.31% |
| 0.1 | 11.19% | 32.92% |
| 0.25 | 18.07% | 0% |
| 0.5 | 25.25% | 0% |

**판정:** 작은 개입에서는 개별 효과의 합으로 설명되는 사례가 많고,
개입을 키우면 그 설명이 깨지는 사례가 늘었다. 다음 비교 실험의 중심 후보는
두 종류의 사례가 함께 나타나는 **0.03·0.06·0.1**이다. 관측한 이산 크기와
개발용 기준점에 대한 결과이며, 그 사이 모든 크기나 새 기준점의 보장은 아니다.
파일에 쓰인 `replication` 라벨도 이번 조사에서는 기존 개발 기준점의 일부다.

검증 결과:

- 5,376개 측정의 누락·중복 및 소스 96개·입력 9개 해시 검사 통과.
- 첫 파일럿과 겹치는 0.25·0.5 크기의 1,792개 행은 집단 라벨을 제외한 모든 값이 동일하다.
- seed 91008의 저장 사례(0.5, 첫 방향 쌍, 양의 부호)를 원래 생성기로 독립 재계산했다.
  기준 출력·개별 반응·혼합 반응 모두 최대 차이 0이다.
- 개별 반응 RMS 합의 최솟값은 약 `1.00e-3`이며 모든 상대값의 분모 판정이 유효했다.
  이는 선언한 절대 허용 오차와의 비교이며, fp32 순전파 오차의 엄밀한 상한은 아니다.

검증 명령:

```bash
python3 research/interpretation_method/verify_pilot.py research/interpretation_method/runs/small_steps_v1_calibration --rerender-seed 91008
```

## 완료 — 미관측 공동 반응 예측 v1

상태: **새 기준점 16개, 미관측 공동 반응 5,376건 평가와 독립 재계산 완료. 확장 기준 미통과.**
아래 조건과 소스 해시를 새 평가 결과 확인 전에 고정했다.

- 과제: 목표 크기의 단독 반응 A·B는 알고, 공동 출력은 모르는 조건에서
  혼합 반응 C와 가산 설명의 허용 여부를 예측한다. 전체 출력이 미관측인 과제와 구분한다.
- 개발: 기존 seed 91001–91008. 평가: 새 seed 93001–93016.
- 관측 가능한 작은 공동 개입은 0.01 한 크기다. 목표는 0.03·0.06·0.1이며
  28쌍·4부호를 유지한다. 상대 10%·절대 `1e-6` 허용 오차를 유지한다.
- 벡터 예측 비교: `C=0`, 국소 혼합 JVP의 2차 예측, 작은 개입 C의
  그대로 사용·선형 외삽·2차 외삽·개발 자료에서 보정한 외삽.
  마지막 네 방법은 같은 유한 관측을 공유한다. 보정 외삽 자체를 신규 기법으로 부르지 않는다.
- 보정은 목표 크기별 양의 계수 하나다. 개발 자료에서 단독 반응 RMS 합으로
  정규화한 벡터 제곱 오차를 최소화하고, 평가 시작 전에 계수를 저장·해시 고정한다.
- 가산 설명 채택 판단에는 개발 자료의 크기별 중앙값 및 단독 반응의
  중첩·크기를 쓰는 ridge 회귀도 비교한다. 이 두 기준은 공동 출력 벡터를 예측하지 않는다.
- 기준점별 평균 상대 공동 반응 오차, 가산 설명 채택률, 잘못 채택한 비율을 함께 낸다.
  오차 차이의 신뢰구간은 기준점 16개를 단위로 한 짝지은 bootstrap 2,000회로 계산한다.
- 각 평가 기준점의 모든 예측을 파일에 저장한 다음 목표 공동 출력을 생성한다.
  평가 자료로 계수·임계값·방법을 다시 선택하지 않는다. 호출 수와 JVP 비용은 따로 기록한다.
- 진행 판정: 보정 외삽이 가산 설명·각 고정 유한 외삽·국소 미분보다 평균 상대 오차를
  각각 5% 이상 줄이고, 짝지은 차이의 95% 구간이 모두 개선 쪽이면 다음 도메인 검증 후보로 삼는다.
  채택 위험이 높으면 안정성 확보로 해석하지 않는다. 성공해도 신규성의 증명은 아니다.

구현 파일은 `prediction.py`, `run_prediction.py`, `verify_prediction.py`,
`test_prediction.py`, `protocol_prediction_v1.json`이다. 해석해·경계 반례·학습/평가
분리 검사, 실제 GPU 사전 검사, 결과 전체 감사와 독립 재계산을 통과했다.

GPU 사전 검사에서 반복·원래 출력 대비 차이는 0, 혼합 미분 순서 교환의 상대 차이는
`4.07e-7`이었다. 혼합 JVP는 건당 약 0.216초, PyTorch 최대 할당 메모리는 약 550 MiB였다.
이는 한 기준점에서 확인한 실행 가능성과 국소 계산의 정합성이다.

비용을 비교할 때 목표 공동 출력 336회/기준점은 정답 측정 비용으로 분리한다.
가산 설명은 기준 출력과 목표 단독 출력 49회를 입력으로 사용한다.
유한 관측 기반 네 방법은 작은 크기의 단독 16회와 공동 112회를 추가로 공유한다.
국소 혼합 미분은 위 49회에 혼합 JVP 28회를 추가한다. 계산량 차이를 숨기지 않는다.

개발 측정 2,688건을 완료했고, 기존 자료와 상대 지표의 최대 차이는 `1.11e-16`이었다.
0.03·0.06·0.1의 보정 계수는 각각 **2.4941·3.9023·5.5663**으로 고정했다.
해석해·실행 순서 검사 19개와 GPU 사전 검사를 통과한 소스로 평가했다.

### 새 기준점 평가 결과

공동 반응 오차는 `RMS(C실제 − C예측) / (RMS(A)+RMS(B))`의 평균이다.
채택은 **원래의 가산 설명**을 10% 허용 오차 안에서 사용할 수 있다고 판단하는 것이다.
오판율의 분모는 채택한 사례 수다. 벡터 보정의 정확도와 채택 판단을 구분해서 읽는다.

| 비교 방법 | 공동 반응 상대 오차 평균 | 가산 설명 채택률 | 채택 중 오판율 |
|---|---:|---:|---:|
| 가산 설명 | 7.702% | 100.00% | 20.15% |
| 국소 혼합 미분 | 7.680% | 99.70% | 19.91% |
| 작은 반응 그대로 | 7.476% | 100.00% | 20.15% |
| 작은 반응 선형 외삽 | 7.258% | 98.88% | 19.24% |
| 작은 반응 2차 외삽 | 19.403% | 24.65% | 0.00% |
| 개발 자료로 보정한 외삽 | 7.230% | 99.67% | 19.88% |
| 개입 크기별 개발 중앙값 | 벡터 예측 없음 | 66.67% | 8.59% |
| 단독 반응 중첩·크기 회귀 | 벡터 예측 없음 | 65.61% | 6.75% |

**판정:** 보정 외삽은 가산 설명보다 평균 오차를 6.13% 줄였다.
그러나 단순 선형 외삽 대비 감소는 **0.39%**이며 기준점 단위 차이의 95% 구간이 0을 포함했다.
사전에 고정한 5% 개선 기준도 만족하지 못했다. 현재 결과로 방법 우위나 안정성 확보를 주장하지 않는다.

2차 외삽의 관측된 오판 0은 채택률 24.65%에서 나온 결과다.
이 자료에서의 채택률·오판율 조합이며 앞으로의 무오판 보장이 아니다.
회귀의 낮은 오판율 역시 채택한 집단이 다르므로 같은 채택률에서의 우위로 해석하지 않는다.

### 원인 진단 — 크기 보정으로 남는 오차

평가 후, 작은 개입 C와 목표 C의 방향 유사도를 진단했다.
코사인 중앙값은 목표 0.03·0.06·0.1에서 **0.540·0.340·0.241**로 낮아졌다.
이번 자료에서는 개입을 키울수록 작은 개입에서 본 혼합 반응의 방향과도 달라졌다.

정답을 알고 사례마다 최적의 양의 계수를 골라도, 단일 작은 반응 방향으로 얻는
평균 상대 오차의 최저값은 **7.140%**다. 고정 보정의 7.230%와 가깝다.
이 제한된 예측 방식에서는 계수만 더 잘 맞춰도 줄일 수 있는 오차가 적다는 진단이다.
정답을 사용한 사후 분석이며 실제 예측 성능이나 일반적인 불가능성 정리로 세지 않는다.

### 재현과 비용

- 평가의 생성 호출 8,208회, 혼합 JVP 448회. 본 평가 약 303.9초.
  그중 순전파 및 CPU 전송 약 158.6초, 혼합 JVP 약 97.3초.
- 소스 100개·입력 9개 해시, 예측 저장 순서 16개, 전체 격자,
  개발 계수 및 기준점 bootstrap 재계산을 통과했다. 저장 텐서 오차는 0이다.
- seed 93016, 목표 0.06의 사례를 원래 조작 함수·생성기 출력으로 독립 재계산했다.
  기준 출력·A·B·C·작은 개입 C 모두 차이 0이며, 기존 AD 래퍼의 혼합 미분 재계산도 차이 0이다.
- 기본 CPU 6스레드 감사는 최대 `5.55e-17`의 합산 차이로 완전 일치 검사에 실패했다.
  측정 소스를 유지하고 실행기와 같은 4스레드로 재검사해 통과했다.

```bash
python3 -m unittest discover -s research/interpretation_method -p 'test_*.py' -v
env OMP_NUM_THREADS=4 MKL_NUM_THREADS=4 python3 research/interpretation_method/verify_prediction.py research/interpretation_method/runs/prediction_v1_evaluation --rerender-seed 93016
python3 research/interpretation_method/analyze_prediction.py research/interpretation_method/runs/prediction_v1_evaluation
```

## 확정 실험 v2 — 두 관측과 보류의 추가 가치

사용자는 방향 설계부터 결론까지 계속 진행하도록 요청했다. 아래 조건은 v2 자료를
생성하기 전에 고정했다. 평가 뒤 방법·임계값을 바꾸지 않았다.

- 학습: 이미 관측한 91001–91008 및 93001–93016, 총 24개 기준점.
- 채택 기준 보정: 새 95001–95016, 16개. 최종 평가: 새 96001–96064, 64개.
  평가 전반·후반 32개도 별도로 보고하되, 64개가 완료되기 전에는 성능을 확인하지 않는다.
- 공동 probe: 0.01·0.03. 목표: 0.06·0.08·0.1. 목표 단독 A/B는 입력으로 허용한다.
  가산 설명의 상대 10%·절대 `1e-6` 허용 기준은 그대로 둔다.
- 예측은 혼합 반응의 크기에 대해 직접 학습한다. 같은 ridge 회귀로
  단독 중첩·크기, 가까운 probe 하나 추가, 두 probe의 크기 추가,
  두 probe의 크기와 방향 일관성 추가를 비교한다. 가까운 probe의 선형·2차 외삽도 유지한다.
- 같은 채택률 비교: 각 기준점·목표 크기에서 예측 위험이 낮은 절반을 선택한다.
  이는 일괄 순위 선택이며 단일 입력에 대한 온라인 보장과 구분한다.
- 고정 채택 규칙: 별도 보정 자료에서 관측 오판율 2.5% 이하인 임계값 중
  채택률이 가장 높은 것을 고른다. 후보는 결과 전에 정한 0–0.15의 151개 격자다.
  이 경험적 보정 자체를 통계적 보장으로 부르지 않는다.
- 실용 목표: 평가에서 채택률 40% 이상, 채택 중 오판율의 기준점 bootstrap
  단측 95% 상한 5% 이하. 독립 기준점 64개를 사용하며 행을 독립 표본으로 세지 않는다.
- 추가 관측의 가치: 두 관측+방향이 가까운 한 관측 및 두 크기만 사용하는 비교 각각에 대해
  위험 크기 예측 오차를 5% 이상 줄이는지, 같은 채택률에서 오판을 1%p 이상 줄이는지 검사한다.
  차이의 95% 구간과 전반·후반 방향을 함께 보고한다. 작은 차이를 실용적 우위로 과장하지 않는다.
- 계산 비용: 기준점당 단독 출력 포함 직접 정답 측정은 385회.
  가까운 probe 하나를 쓰는 예측은 177회, 두 probe 예측은 305회다.
  두 관측의 입력 비용 증가가 추가 가치로 정당화되는지도 판정한다.
- 종료 기준: 64개 평가와 독립 재현 후, 각 후보를 추가 가치 확인/기준 미충족/자료로 미확정으로 판정한다.
  효과 크기의 신뢰구간이 판단 경계를 걸치면 설정을 바꾸지 않은 독립 확인 표본을 추가한다.
  최종 결론은 검증한 모델·방향·개입 범위까지다. 일반적 신규 이론의 성공을 미리 결론내리지 않는다.

보류를 통한 위험·채택률 절충은 기존
[선택적 예측 연구](https://arxiv.org/abs/1705.08500)의 문제 설정이다.
[Conformal Risk Control](https://arxiv.org/abs/2208.02814)의 단조 손실 기대값 보장을
채택 집단의 조건부 오판율이나 서로 종속된 방향 쌍에 그대로 적용하지 않는다.
이번 실험에서는 경험적 기준점 신뢰구간과 수학적으로 보장되는 내용을 구분한다.

구현·검증 대상: `selective.py`, `run_selective.py`, `verify_selective.py`,
`test_selective.py`, `protocol_selective_v2.json`. 기존 측정 소스는 유지한다.

진행 상태: **학습 8,064건·별도 보정 5,376건·새 평가 21,504건과 전체 재귀 감사 완료.**
기준점 64개 평가 후 사전에 지정한 판단 경계가 구분돼 확인 표본을 추가하지 않았다.
고정된 채택 임계값은
overlap 0.074, near_ridge 0.094, two_amplitude/two_direction 0.095,
near_linear 0.051, near_quadratic 0.149다. 최종 평가 성능에 맞춰 바꾸지 않는다.

해석해·전체 실행 흐름·자료 분리 검사 **27개 통과**. 감사 진입점에서 CPU 스레드 수를
설정하고, 수치 허용 기준을 명시해 v1의 마지막 비트 비교 문제도 재발하지 않게 했다.

판정의 구체화: '5% 개선 확인'은 개선율의 95% 구간 하한이 5% 이상인 경우,
'5% 개선 근거 배제'는 상한이 5% 미만인 경우다. 같은 채택률 오판 차이의 1%p도
같은 방식으로 구분한다. 필요시 확인 표본은 미리 예약한 98001–98064이며,
설정을 바꾸지 않고 독립적으로 평가한다. 데이터가 지지하지 않는 일반적 결론은 내리지 않는다.

확인 표본을 여는 조건은 near_ridge/two_amplitude 대비 두 방향 방식의 5% MAE 또는
1%p 오판 개선 경계를 95% 구간이 걸치는 경우다. 실용 후보가 채택률 40% 이상이면서
조건부 오판율의 95% 구간이 5% 경계를 걸치는 경우도 포함한다.
`decide_selective.py`가 이 조건을 판정한다. 확인 표본까지도 불명확한 경계는 미확정으로 남기며
원하는 결론을 얻기 위한 반복 조정은 하지 않는다.

보조 비교도 평가 전에 지정한다: `overlap → near_ridge → two_amplitude → two_direction`의
정보 추가 단계별 차이, 그리고 view 방향을 제외한 결과를 확인한다.
`analyze_selective.py`는 이 분석만 수행하며 예측기·채택 임계값을 다시 맞추지 않는다.

### 수학적으로 구분되는 두 결론

**추가 가정이 없으면 유한 관측만으로 미관측 개입을 보장할 수 없다.**
관측한 입력들의 유한 집합을 S, 그 밖의 목표 공동 입력을 x*라 하자.
x* 주변에 S와 겹치지 않는 작은 공을 잡고, 그 안에서만 값이 있는 매끄러운 bump 함수
b를 `b(x*)=1`이 되게 만들 수 있다. 임의의 출력 벡터 v에 대해
`F1(x)=F0(x)+b(x)v`는 S의 모든 출력과 그 점들의 모든 국소 미분을 유지하면서
목표 출력만 v만큼 바꾼다. 따라서 일반 함수에 대한 보장은 유한 관측만으로 식별되지 않는다.
이는 고전적인 미관측 구간 반례 논리이며 새로운 불가능성 정리로 주장하지 않는다.
고정된 모델의 전체 구조를 사용한 별도 인증이나 실제 목표 출력 계산을 배제하는 정리도 아니다.

**정량적 가정이 있으면 조건부 오차 상한은 쓸 수 있다.**
부호를 고정한 두 개입 좌표에서 혼합 미분 H가 전체 사각형에 걸쳐 존재하고,
출력 RMS 노름에 대해 `||H(u,v)-H(u',v')|| ≤ L(|u-u'|+|v-v'|)`라고 하자.
`C(s)=s²∫₀¹∫₀¹ H(su,sv) du dv`이므로 `0<p<t`에서

`||C(t) − (t/p)² C(p)|| ≤ L t² (t−p)`.

우변과 예측 C의 노름 합이 허용 오차 이하면 가산 설명을 인증할 수 있고,
판정이 겹치면 보류할 수 있다. 그러나 **작은 관측 두 개는 전체 구간의 L을 인증하지 않는다.**
현재 생성기의 활성화 경계를 포함해 이 가정을 확보하지 않았으므로, 이 수식을
이번 경험적 회귀의 안전 보장으로 사용하지 않는다. 매끄러운 숨은 bump 반례와
상한에 등호가 되는 다항식 예는 실행 검사에 포함했다.

### v2 평가 결과와 판단

MAE는 `RMS(C)/(RMS(A)+RMS(B))` 예측값의 평균 절대 오차다.
가산 설명의 실제 허용 여부는 기존 10% 상대·`1e-6` 절대 기준으로 판단했다.
아래 채택률은 **별도 자료로 고정한 임계값**의 결과이며, 오판율의 분모는 채택한 사례 수다.

| 방법 | 위험 크기 MAE | 채택률 | 채택 중 오판 | 단측 95% 오판 상한 | 기준점당 입력 호출 |
|---|---:|---:|---:|---:|---:|
| 단독 중첩·크기 회귀 | 0.02622 | 18.25% | 8.23% | 11.39% | 49 |
| 가까운 관측+회귀 | 0.00705 | 56.50% | 2.43% | 3.05% | 177 |
| 두 관측의 크기+회귀 | 0.00629 | 57.22% | 2.35% | 2.91% | 305 |
| 두 관측의 크기·방향+회귀 | 0.00626 | 57.22% | 2.36% | 2.92% | 305 |
| 가까운 관측 선형 외삽 | 0.03706 | 40.47% | 1.19% | 1.66% | 177 |
| 가까운 관측 2차 외삽 | 0.07190 | 52.09% | 3.17% | 4.13% | 177 |

**기본 방법 판정:** 가까운 관측+회귀는 12,149건을 채택해 295건에서 허용 오차를 넘었다.
전반 32개에서는 채택 59.55%·오판 1.92%, 후반 32개에서는 53.44%·2.99%로
각각 실용 목표를 통과했다. view 제외 16,128건에서도 채택 49.78%·오판 1.62%,
단측 상한 2.19%로 통과했다. 선형 외삽은 후반 및 view 제외에서 채택률 40%를 못 채웠고,
2차 외삽은 view 제외 오판 상한이 5.11%로 목표를 넘었다.

추가 정보의 효과는 별도로 판정했다.

- 가까운 관측 추가: 단독 중첩·크기 회귀 대비 MAE **73.11% 감소**,
  95% 구간 [68.81%, 77.08%]. 같은 50% 채택에서 오판 감소 **6.99%p**, 구간 [5.63, 8.48]%p.
- 두 번째 관측의 크기: 한 관측 대비 MAE **10.75% 감소**, 구간 [7.44%, 13.27%].
  같은 채택률의 오판 감소는 **0.056%p**, 구간 [−0.093, 0.233]%p로 1%p 실용 개선을 뒷받침하지 않는다.
- 방향 일관성: 두 크기만 쓸 때보다 MAE **0.46% 감소**, 구간 [0.21%, 0.67%].
  작은 차이는 관측되지만 5% 실용 효과는 배제된다. 같은 채택률 오판 감소도 0.047%p로 작다.

따라서 **채택·보류 판단의 기본 구성은 한 관측**, 위험 크기 자체의 정밀도가 필요하면
**두 크기 관측을 선택적으로 사용**하는 방향으로 정한다. 방향 특징을 새 핵심 이론으로 삼을 근거는 부족하다.
두 관측은 생성 호출을 72.32% 늘린다. 기본 구성의 177회는 모든 목표를 직접 측정하는
385회보다 54.03% 적다. 이 수치는 단독 출력을 공유하는 전체 격자의 순전파 호출 비교이며,
학습·보정 비용과 다른 모델의 실행 시간을 보장하는 수치는 아니다.

### 보류가 필요한 이유

각 기준점·목표 크기에서 무조건 절반을 고르는 순위 실험에서는 가까운 관측+회귀도
오판율이 22.20%였다. 사후 정답을 사용해 계산하니, 192개 집단 중 68개에는
실제로 허용되는 사례가 절반보다 적었다. **정답을 아는 최적 선택도 이 규칙에서는
오판율 21.28% 아래로 갈 수 없다.** 이는 예측 성능이 아닌 유한 자료의 정답 기반 하한이다.

고정 위험 임계값을 쓰면 어려운 기준점·크기에서 더 많이 보류하고, 쉬운 곳에서는 더 많이
채택할 수 있다. 이 설정에서 전체 채택 56.50%와 오판 2.43%를 얻었다.
좋은 순위와 낮은 절대 오판 위험을 구분해야 한다는 결과다.

### 검증 완료와 실행

- 실제 구현의 해석해·반례·자료 분리·전체 toy 흐름 검사 27개 통과.
- 학습/보정/평가의 프로토콜, 소스 101개, 입력 9개와 주요 결과 파일·예측 저장 해시 감사 통과.
  평가 64개에서 예측 저장 뒤 목표 공동 출력을 생성한 순서를 검증했다.
- 저장 사례 64개의 특징·위험값을 재계산했다. seed 96064, 목표 0.08의 기준 출력,
  A·B·C 및 두 probe C를 원래 조작 함수·생성기로 재계산해 모두 최대 차이 0을 확인했다.
- 기본 방법의 별도 실행 `explain.py`는 작은 공동 관측 **0.03만** 썼다.
  177회 호출, 목표 공동 출력 호출 0회로 seed 96064의 336개 점수·판단을 완전히 재현했다.
  이미 평가한 사례의 구현 검증이며 새 성능 표본으로 세지 않는다.
- v2 본 측정 생성 호출: 학습 15,384회, 보정 10,256회, 평가 41,024회.
  평가 본 측정 약 938.3초. 추가 확인의 조건이 충족되지 않아 98001–98064는 사용하지 않았다.

```bash
python3 research/interpretation_method/verify_selective.py research/interpretation_method/runs/selective_v2_evaluation --rerender-seed 96064
python3 research/interpretation_method/explain.py --seed 96064 --out research/interpretation_method/runs/near_explainer_96064
```

재실행에는 새 출력 디렉터리를 사용한다. 결과의 `adopt_additive`는 보정한 설명 채택,
`abstain`은 설명 보류다. 보류된 사례를 설명해야 하면 목표 공동 반응을 직접 측정한다.
이 방법은 목표 단독 A/B를 이미 아는 조건에서 0.06·0.08·0.1의 가산 설명 허용 여부를 선별한다.

## 확정한 연구 방향과 주장 범위

연구의 중심은 **유한 개입의 반응 분해와 검증된 적용 범위, 그리고 선택적 설명**으로 둔다.
복잡한 방향·곡률 지표의 보편적 우위를 중심 주장으로 삼지 않는다.
기본 방법과 조건, 기준점 단위 검증, 실패할 때의 보류를 함께 제시하는 방법론으로 정리한다.

이번에는 제한된 범위의 실용 목표와 정보 추가 효과에 대한 결론을 얻었다.
관측 크기를 더 늘리거나 평가 seed를 계속 바꿔 원하는 효과를 찾는 실험은 종료한다.
다른 모델·출력 표현·인코더 입력으로 확장하려면 그 환경의 개입 단위와 보정을 별도로 검증해야 한다.
현재 96001–96064는 이후 새 평가로 재사용하지 않는다.

논문으로 주장 가능한 후보는 이 검증 절차와 적용 범위에 대한 실증·한계 분석이다.
외삽·ridge·보류 자체의 신규성, 의미 정확도, 분포에 무관한 위험 보장이나
보편적인 새 해석 이론을 확보했다고 주장하지 않는다.

## 근거와 실행 파일

- [기존 실험을 반영한 방향 설계](research/interpretation_method/DIRECTION.md)
- [수학 정의](research/interpretation_method/DESIGN.md)
- [첫 파일럿 결과](research/interpretation_method/PILOT_REPORT.md)
- [측정 코드](research/interpretation_method/measure.py)
- [실행 방법](research/interpretation_method/README.md)
- [작은 개입 범위 조사 프로토콜](research/interpretation_method/protocol_small_steps_v1.json)
- [작은 개입 결과 요약](research/interpretation_method/runs/small_steps_v1_calibration/summary.json)
- [독립 재계산 검증](research/interpretation_method/runs/small_steps_v1_calibration/verification_rerender.json)
- [이전 실험과의 일치 검사](research/interpretation_method/runs/small_steps_v1_calibration/consistency_verification.json)
- [미관측 공동 반응 예측 프로토콜](research/interpretation_method/protocol_prediction_v1.json)
- [개발 자료로 고정한 예측 계수](research/interpretation_method/runs/prediction_v1_training/model.json)
- [미관측 예측 결과와 기준점 신뢰구간](research/interpretation_method/runs/prediction_v1_evaluation/summary.json)
- [미관측 예측 감사와 독립 재계산](research/interpretation_method/runs/prediction_v1_evaluation/verification_rerender.json)
- [단일 반응 방향의 사후 한계 진단](research/interpretation_method/runs/prediction_v1_evaluation/projection_diagnostic.json)
- [v2 고정 프로토콜](research/interpretation_method/protocol_selective_v2.json)
- [v2 새 기준점 평가](research/interpretation_method/runs/selective_v2_evaluation/summary.json)
- [v2 정보 추가·view 제외 분석](research/interpretation_method/runs/selective_v2_evaluation/secondary_analysis.json)
- [v2 확정 판정](research/interpretation_method/runs/selective_v2_evaluation/decision.json)
- [v2 전체 감사·독립 재계산](research/interpretation_method/runs/selective_v2_evaluation/verification_rerender.json)
- [고정 채택률의 사후 정답 하한](research/interpretation_method/runs/selective_v2_evaluation/coverage_limit.json)
- [기본 실행의 검증](research/interpretation_method/runs/near_explainer_96064/verification.json)

실험 코드와 원본 결과는 `research/interpretation_method/` 아래에 유지한다.
기존 논문과 과거 실험 기록은 별도로 보존한다.
