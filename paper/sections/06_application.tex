% =====================================================
% 06_application.tex — ~1 pg
% =====================================================
\todo{compositional editing prediction + saliency-guided local edit user study}

\subsection{Predicting compositional editing failure}
Using the predictor $P(a, b)$ derived from the mixed Hessian, we ask:
given two attribute boundaries $a, b$ in a domain we have not yet
tested compositionally, can we predict ahead of time whether they
will compose linearly or interfere?

We hold out 10 of 28 bedroom pairs as a test set, compute the
predictor on the remaining 18, fit a single threshold, and report
test-set accuracy. \todo{numbers and table go here.}

\subsection{Saliency-guided local editing: user study}
For 30 image-attribute pairs in each domain, we present human raters
on Prolific with three editing outputs:
(a) baseline image,
(b) global edit at $\alpha = +3$,
(c) saliency-mask-guided local edit at $\alpha = +3$.
Each rater (n = $\geq 60$) rates "preferred edit" between (b) and (c)
and provides a free-text rationale. \todo{report preference rates
and significance.}

\subsection{Unsupervised attribute discovery + CLIP labelling}
We use the C6 pipeline to discover new attribute candidates in
each domain, label them via CLIP zero-shot, and propose them as
boundary directions for future labelling efforts. Examples are shown
in the supplementary.
